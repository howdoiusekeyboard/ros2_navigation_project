# Backend Server Dependencies
# Python 3.10+

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.26
websockets==12.0

# OpenAI (Whisper API)
openai==2.13.0

# Google AI (Gemini - server-side validation)
google-genai==1.52.0

# ROS2 Integration
# IMPORTANT: ROS2 packages MUST be installed via apt, NOT pip!
# Install with: sudo apt install ros-humble-rclpy ros-humble-geometry-msgs ros-humble-nav-msgs
# These packages are imported in Python but installed system-wide
# rclpy (ROS2 Python client library)
# geometry_msgs (Twist, Pose messages)
# nav_msgs (Odometry)
# sensor_msgs (LaserScan)
# nav2_msgs (NavigateToPose action)

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9  # PostgreSQL driver
alembic==1.13.1  # Database migrations
aiosqlite==0.19.0  # Async SQLite for conversation memory

# Vector Embeddings (for context/memory)
sentence-transformers==2.3.1
numpy==1.26.3

# Utilities
python-dotenv==1.0.1
pydantic>=2.9.0
pydantic-settings==2.1.0

# Async support
aiofiles==23.2.1
httpx>=0.28.1

# Logging
loguru==0.7.2

# Testing (development)
pytest==9.0.3
pytest-asyncio==0.23.3
